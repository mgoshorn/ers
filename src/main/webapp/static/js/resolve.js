let resolve = function(id) {
    let element;

    //TODO get element

    let resolution;

    let getResolution = function() {
        //TODO get resolution
    }

    let createBody = function(id, resolution) {
        body = {
            "id": id,
            "status": resolution
        };
        return body;
    }

    let json = createBody(id, resolution);
    

}